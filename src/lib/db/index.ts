/**
 * Database access layer (repositories).
 *
 * All Supabase queries are encapsulated here so components and Server Actions
 * never scatter raw queries. One repository per domain (profiles, projects,
 * quotes, …) will be added as features are implemented in later phases.
 */
